import json
with file.open('./data/output.json', 'r') as data:
    pile = data.read()
with open('./data/output.json', 'r') as data:

    pile = data.read()
pile
pile = pile.split('\n\n')
for p in pile:
    p = p.strip()
pile
for p in pile:
    p = json.loads(p)[0]
type(pile[0])
for p in pile:
    p = json.loads(p[0])
for p in pile:
    p = json.loads(p)
pile
type(pile[0])
json_data = {}
json_data = []
for p in pile:
    json_data.append(json.loads(p))
type(json_data[0])
